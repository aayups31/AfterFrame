# Codex Phase 1B — Investigation Stream

Implement the mocked exploration stream using typed beats from `src/lib/mock-case.ts`.

Required beat behaviors:

- opening;
- context;
- evidence;
- turn;
- contradiction;
- question;
- connection;
- lead;
- resolution.

Use one clean reading surface with editorial margins. Do not wrap each beat in a visible card. Implement scroll reveals with progressive enhancement and `prefers-reduced-motion` support.

Add:

- source whispers;
- source inspector;
- exact-locator display;
- open-original affordance;
- active trail marker;
- resumable scroll position.

Run build and interaction tests. Verify keyboard access to every evidence hint.
