# Codex Phase 4 — Notes, Connections, and World View

Implement the user-created world.

Required views:

- narrative path;
- evidence relationships;
- timeline;
- user notes;
- unresolved leads;
- cross-case portal.

Use `@xyflow/react` only as an interaction engine. Replace default rectangular nodes with restrained text labels, thin lines, uncertainty halos, and focus details. Lazy-load this surface.

Implement:

- accepted versus proposed edges;
- explicit connection decisions;
- note anchor survival;
- one mocked prior-case connection;
- export to JSON and Markdown.

The result must still look like AFTERFRAME, not a generic node editor.
