# Codex Phase 14 — Companion Extension Design Only (Deferred)

Do not implement an extension.

Read `docs/29-research-companion-extension.md` and produce only:

- an ADR for browser permissions;
- context-packet schemas;
- threat-boundary diagrams;
- a site-adapter interface;
- a future monorepo package plan;
- an explicit list of proof gates required before implementation.

Any generated extension code is out of scope until the standalone Movie Investigator passes retention and workflow-pull gates.
