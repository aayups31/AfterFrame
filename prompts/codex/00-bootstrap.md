# Codex Phase 0 — Bootstrap

Read `README.md`, `AGENTS.md`, and every file in `docs/` before changing code.

Your task is to audit the starter repository against the product laws, then produce a concrete implementation plan for Phase 1 only: the premium mocked Black Hawk Down vertical slice, including the direction console, one branch transition, and closure preview.

Do not implement yet.

Your plan must include:

- current repository map;
- reusable components;
- missing states;
- accessibility and reduced-motion plan;
- exact files to change;
- test plan;
- visual risks;
- a sequence of commits small enough to review.

Do not propose cards, dashboards, 3D scenes, or a central chat interface. Preserve the typography-first, editorial investigation surface.
